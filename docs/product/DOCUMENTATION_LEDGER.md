# Product Documentation Ledger

This is the append-only change ledger for canonical product documentation.
Every request that creates or updates product documentation adds one dated row.
Use one row per coherent request, not one row per file.

| Date | Area | Change type | Status | Summary | Affected documentation | Code/tests verified |
|---|---|---|---|---|---|---|
| 2026-09-07 | Predictions / league hub TABLE team names | Visual hierarchy | BUILT | Kept every club name in the standings table white (`AppTheme.whiteColor`) for a consistent readable label hierarchy; club identity colour remains on the team crest and qualification accents. | `systems/predictions.md` | `lib/screens/predictions/widgets/standings_table.dart`; targeted `flutter analyze`, focused `league_stats_package_service_test.dart`, and a successful Flutter web boot check (HTTP 200) |
| 2026-08-24 | Hoop Duel / basketball cards | Asset mapping | BUILT | Added 55 labeled supplied basketball portraits and mapped each exact current-roster athlete ID to its collectible card; unmatched roster cards keep the existing icon fallback. | `games/hoop-duel.md` | `lib/data/basketball_portraits.dart`, `lib/models/cards.dart`, `test/basketball_portraits_test.dart`; targeted Flutter analyze, portrait bundle test, and local Flutter web smoke-server check |
| 2026-08-09 | Whole app | Overhaul | BUILT / PROTOTYPE / PLANNED | Reorganized the product library; documented all 18 Games-tab entries; added progression, streak, achievement, social, economy, collection, support, and design coverage; corrected current behavior; archived superseded root documents. | `README.md`, `games/`, `systems/`, `design/`, `DOCUMENT_TEMPLATE.md` | App navigation and Games registry; progression, streak, achievement, storage, picks, prediction, referral, friends, daily-mystery and game-mode sources; targeted tests listed on feature pages; refreshed stale catalog coverage to include racing cards |
| 2026-08-11 | Onboarding / achievements | Feedback adjustment | BUILT | Suppressed the Treasury global achievement reveal so the one-time 1,000-coin welcome animation remains the sole first-run reward moment; the badge still unlocks and is recorded as celebrated. | `systems/achievements.md`, `systems/profile-onboarding-and-settings.md` | `lib/blocs/achievement/achievement_celebration_controller.dart`; `test/achievement_celebration_controller_test.dart`; targeted Flutter analyze and onboarding/celebration tests |
| 2026-08-15 | Cricket Quiz | Content correction | BUILT | Replaced every fictional score and arithmetic scenario with audit-backed factual cricket trivia while retaining the 2,000-question ladder, mode coverage, and rewards. | `games/cricket-quiz.md` | `tool/generate_cricket_quiz.dart`; `tool/verify_quiz_bank.dart`; `test/quiz_cubit_test.dart`; targeted quiz tests and Flutter analyze |
| 2026-08-17 | Deck Locker (Profile → All Decks) | Feature rework | BUILT | Rebuilt the Deck Locker as a sport-tabbed hub matching the GAMES page's sport strip: each tab shows the real equipped squad as cards with a fill meter and EDIT/BUILD LOADOUT CTA; a tab whose starter pack is unclaimed shows a locked state whose PLAY \<SPORT\> action routes to that sport's GAMES tab (claiming the starter pack there fills the loadout); removed the SQUAD CHANNEL multi-squad pill row and New Squad action from this screen (multi-squad management now lives only in the football deck builder). | `systems/collections-decks-and-packs.md`, `systems/profile-onboarding-and-settings.md` | `lib/screens/deck/all_decks_screen.dart`, `lib/screens/profile/profile_screen.dart`, `lib/app.dart`; `test/deck_locker_widget_test.dart` (rewritten for the tabbed UI), `test/shared_deck_locker_test.dart`; `flutter analyze` on all changed files; manual Playwright pass on `flutter build web --release` covering all five locked tabs, the PLAY FOOTBALL → GAMES route, claiming the football starter pack, and the resulting unlocked tab (real squad cards + EDIT LOADOUT → football deck builder) |
| 2026-08-17 | Basketball Quiz | Content implementation | BUILT | Added the full audited 2,000-question Basketball Quiz ladder across Easy, Medium, Hard, and Global, with NBA-led worldwide coverage, exact difficulty-band scope mixes, and facts frozen at 17 August 2026. | `games/basketball-quiz.md` | `tool/generate_basketball_quiz.dart`; `tool/verify_quiz_bank.dart`; `test/quiz_cubit_test.dart`; `test/quiz_set_flow_test.dart`; targeted Flutter analyze; Flutter web compile/server smoke and rendered widget-flow verification |
| 2026-08-18 | Tennis Rally / Shop | Asset mapping | BUILT | Added 47 supplied tennis athlete portraits and mapped each by roster ID so they render in tennis collectible-card surfaces and purchasable shop avatars; unsupplied roster athletes continue to use the existing fallback. | `games/tennis-rally.md`, `systems/economy-shop-and-ledgers.md` | `lib/data/tennis_portraits.dart`; `lib/models/cards.dart`; `test/tennis_portraits_test.dart`; targeted Flutter analyze and asset-bundle test |
| 2026-08-19 | Motorsport Quiz (was F1 Quiz) | Content implementation | BUILT | Added the full audited 2,000-question Motorsport Quiz ladder across Easy, Medium, Hard and Global, covering F1, MotoGP, NASCAR, IndyCar, endurance, rally and feeder series with exact per-mode scope mixes and an era-banded difficulty ramp reaching back to the 1911 Indianapolis 500 and the 1923 Le Mans. Unlike the cricket and basketball banks, all 2,000 questions are distinct recorded facts with no re-worded variants, and the verifier now enforces that. Facts frozen at 19 August 2026 (WRC stops at the 2024 title). Renamed the Games tile and page from F1 Quiz to Motorsport Quiz to match the wider content. | `games/motorsport-quiz.md` (renamed from `games/f1-quiz.md`), `README.md` | `tool/generate_motorsport_quiz.dart`; `tool/verify_quiz_bank.dart` (new motorsport audit spec and `requireDistinctFacts` guard, negative-tested); `lib/screens/predictions/prediction_home_screen.dart`; `test/quiz_cubit_test.dart`, `test/quiz_set_flow_test.dart`, `test/game_hero_cta_test.dart` (37 tests pass); targeted `flutter analyze` clean on all six changed files |
| 2026-08-21 | Tennis Quiz | Content implementation | BUILT | Added the full audited 2,000-question Tennis Quiz ladder across Easy, Medium, Hard and Global, completing Knowledge Arena coverage for all five sports. Deliberately men-leaning as requested: the ATP scope is 865 questions (~43% alone, over half once the men's share of majors, doubles and team events is counted) while the WTA scope holds a fixed fifth of every band at 425, with majors and flagship events 215, team events 190, doubles 145, wider tennis world 95 and rules/terminology 65. Unlike Motorsport, tennis bands are ordered by fame rather than chronology - Borg, McEnroe, Navratilova and Court are household names, so a 1980 Wimbledon final sits in band 1 - and every singles/doubles prompt names its discipline so no question is ambiguous between the draws. All 2,000 questions are distinct recorded facts with no re-worded variants, enforced by the verifier. Facts frozen at 21 August 2026 with the tour record authored through the end of the 2025 season (no 2026 results, which fell outside primary-source verification at the cutoff). The Games tile, lobby, ladder and XP wiring already existed and were unchanged; the tile is no longer a dead end. | `games/tennis-quiz.md` (rewritten from the legacy short format onto the shared quiz skeleton), `README.md` | `tool/generate_tennis_quiz.dart` (new, 2,000 authored facts, every one used); `assets/quiz/tennis_{easy,medium,hard,global}.json` and `tool/quiz_audit/tennis.json` (generated); `tool/verify_quiz_bank.dart` (new tennis audit spec at the strictest bar - `requirePrimarySource`, `requireContextMetadata` and `requireDistinctFacts` - passing 10,000 questions across all five sports, exit 0); `lib/services/quiz_bank.dart` (new `debugSetPool` test seam, the only lib change); `test/quiz_cubit_test.dart` and `test/quiz_set_flow_test.dart` (tennis added to the banked-sport loops; the two tests that used tennis as the unauthored-sport sentinel re-pointed at a forced empty pool; new tennis render and flawless-set clones), `test/game_hero_cta_test.dart`, `test/tennis_widget_test.dart` (47 tests pass); full `flutter test` suite; targeted `flutter analyze` clean on all changed files |
| 2026-08-22 | Match hub search | Feature implementation | BUILT / PROTOTYPE DATA | Added a fixed MATCH-strip search action and a cyber-styled all-sports finder that searches available fixture feeds by team or league name/code, reports scan and partial-feed states, and opens matching fixture cards without changing the underlying sport/day selection. | `systems/predictions.md`, `README.md` | `lib/screens/predictions/match_search_screen.dart`, `lib/screens/predictions/prediction_home_screen.dart`, `lib/blocs/prediction/prediction_cubit.dart`, shared cyber search/sport-strip widgets; `test/match_search_screen_test.dart`, `test/prediction_home_day_navigation_test.dart`, targeted Trending assertion; targeted Flutter analyze and Flutter web compile/serve smoke |
| 2026-08-23 | Football Bingo | Content expansion | BUILT | Grew the daily career-grid season from 50 slots (really only 3 hand-authored blueprints stretched via axis rotation) to 200 independently authored real grids, one per campaign day, so a full 200-day run never repeats the old padding trick. Added 186 new curated player career records (213 total) and 38 new club labels spanning the top five European leagues plus Portugal, the Netherlands, Turkey, Scotland, South America, MLS, and Saudi Arabia. Day-mapping architecture is unchanged (per-user relative day index into the puzzle list, not a fixed calendar epoch) — only `kFootballBingoCampaignLength` (50→200) and `kFootballBingoContentVersion` (2→3, forcing a clean season on the existing v1→v2 migration path) changed. | `games/football-bingo.md` | `lib/data/football_bingo_puzzles.dart`, `lib/data/football_bingo_careers.dart`, `lib/models/football_bingo.dart`; `test/football_bingo_test.dart` (season-validity test runs `validateFootballBingoPuzzle` + `validateFootballBingoCareerPuzzle` over all 200 grids; fixed a scroll-budget assertion sized for the old 50-day list); full suite passes (14/14); targeted `flutter analyze` clean on all changed files |
| 2026-08-24 | Predictions / Trending feed (Picks market tiles) | Card design polish | BUILT | Fixed the FUTURE/PICK market tile's question text truncating mid-word at 1 line in its square layout (now wraps to 2 lines, with a `CyberBentoGrid` row-height floor bump for safety); replaced the `_OutcomeSignal` outcome bar's hand-rolled fill with the shared `CyberProgressBar`; added a pulsing "hot" delta chip (reusing `CyberPulse`) when a non-settled market's leader swings ≥5 percentage points on the latest tick. `_TrendingMatchCard`/`_TrendingPredictCard` were left unchanged (no free-text truncation issue; no price-history equivalent on `SportMatch`). | `systems/predictions.md` | `lib/screens/predictions/widgets/trending_match_bento.dart`; targeted `flutter analyze` clean; manual Playwright pass on `flutter build web --release` covering the square (default), temporarily-forced wide, and temporarily-forced tall layouts, plus a temporarily-lowered hot-delta threshold to confirm the pulse chip and the settled-market suppression both render correctly (catalog/threshold reverted after) |
| 2026-08-25 | Hoop Duel / basketball cards | Asset mapping | BUILT | Added 14 supplied portraits for previously unmapped current-roster athletes, raising collectible-card portrait coverage from 55 to 69. Two supplied blank placeholder images remain on the icon fallback. | `games/hoop-duel.md` | `lib/data/basketball_portraits.dart`, `test/basketball_portraits_test.dart`; targeted Flutter analyze and portrait bundle test |
| 2026-08-27 | Predictions / football match stats | Feature revamp | BUILT / PROTOTYPE DATA | Rebuilt football's match STATS surface around a normalized bundled Fulham–Chelsea package, with overview intelligence, a 98-sample signed momentum graph, 23-event timeline, confirmed 20-player squads, and 104 commentary entries while preserving partial ESPN states. | `systems/predictions.md` | Football package model/decoder, `FootballMatchStatsView`, and lineup HUD; decoder/widget/match-detail tests, targeted Flutter analyze, and Flutter web build/server smoke (interactive browser sign-off pending because no browser surface was connected) |
| 2026-08-28 | Predictions / basketball and cricket match stats | Feature revamp | BUILT / PROTOTYPE DATA | Added normalized bundled NBA Finals and IPL Final fixtures and dedicated five-section report HUDs: basketball win probability, scoring map, plays, 30-player box score and availability; cricket late-chase rates, complete innings scorecards, commentary/notes, and 24-player squads. Existing ESPN and other sport paths retain contextual partial-data handling. | `systems/predictions.md` | Basketball/cricket package models and decoders, sport match stats views, shared HUD panel, enriched scorecards, and repository registration; decoder/widget/match-detail/outcome tests, targeted Flutter analyze, and Flutter web build/server smoke (interactive browser sign-off pending because no browser surface was connected) |
| 2026-08-29 | Predictions / basketball and cricket fixture board | Prototype fixture scheduling | BUILT / PROTOTYPE DATA | Kept the supplied NBA Finals Game 5 and Royal Challengers Bengaluru v Gujarat Titans completed reference fixtures on each player's local TODAY board by rebasing their bundled kickoff calendar dates on every catalog read while preserving their supplied times and full match reports. | `systems/predictions.md` | `lib/services/prediction_repository.dart`; `test/basketball_cricket_match_package_service_test.dart`; targeted Flutter test and analyze |
| 2026-08-30 | Collections / cricket player cards | Asset mapping | BUILT | Added 30 supplied labelled cricket portraits to the canonical cricket portrait bundle. The existing display-name resolver maps each supplied image to its matching player card; cards without a supplied portrait retain their current fallback. | `systems/collections-decks-and-packs.md` | `assets/cricketer_images/`, `lib/models/cards.dart`, `test/cricket_portraits_test.dart`; targeted Flutter analyze, portrait bundle test, and production Flutter web build |
| 2026-08-30 | Predictions / cricket match stats | Scoring race | BUILT / PROTOTYPE DATA | Replaced the late-chase rate trace with a two-team full-innings cumulative scoring race, backed by the verified GT–RCB over progression while retaining the compact late-chase Match Feed. | `systems/predictions.md` | Cricket package models/decoder, `CricketMatchStatsView`, bundled fixture data, and decoder/widget tests; targeted Flutter analyze and widget tests |
| 2026-08-30 | Predictions / football, cricket and basketball match stats; Picks / market detail; Cyber UI design system | Feature revamp | BUILT / PROTOTYPE DATA | Rebuilt the three sport STATS views on the pick market-detail language. Extracted that screen's chart treatment into a shared `cyber_chart.dart` (`CyberChartPanel`, `CyberChartPainter`, `ChartSeries`, `ChartMarker`, `CyberChartFullScreen`) and refactored market detail to consume it, so picks and stats are one system rather than a copy. Every graph is now drag-to-scrub with selection haptics, a dashed playhead, range tabs, a live per-series legend with context (minute / over / game clock), and expand-to-fullscreen: football MOMENTUM (FULL/1ST/2ND HALF, goal markers), cricket RACE (20 OV/POWERPLAY/DEATH, wicket markers, scrub reads both scoreboards at one over), basketball FLOW (GAME/H1/H2/CLUTCH, turning-point markers) and the scoring map (ALL/HOME/AWAY/3PT). Restored the previously dead cricket CHASE tab as a second chart (actual vs required run rate). Added a match pulse hero to every OVERVIEW, replaced repeated `SYS://` telemetry panels with hairline section headings, and moved stat/event/roster rows onto the shared outcome-row shell. Promoted `CyberSectionHeading`, `CyberMiniMetric`, `CyberStatPill` and `CyberDeltaChip` into the shared catalog and added an `AppTheme.chartSurface` token to replace hardcoded `0xff10192d`. Fixed defects found in running-app review: the chart axis was hard-clamped to 0-100 (cricket run totals drew outside the plot), dense wicket markers drew a full-height cage, an all-positive axis padded below zero, a long cricket score line blew out the hero row, and hero/intel panels duplicated venue and status. | `systems/predictions.md`, `design/cyber-ui-design-system.md`, `README.md` | `lib/widgets/cyber/cyber_chart.dart`, `lib/screens/predictions/widgets/match_stats_shell.dart`, the three `*_match_stats_view.dart` files, `lib/screens/predictions/market_detail_screen.dart`, `lib/widgets/cyber/cyber_widgets.dart`, `lib/config/theme.dart`; `test/cyber_chart_test.dart` (new), `test/football_match_stats_view_test.dart`, `test/basketball_cricket_match_stats_view_test.dart`, `test/match_detail_screen_test.dart`, `test/picks_widget_test.dart`; clean `flutter analyze lib/ test/`, full `flutter test` diffed against a stashed baseline (no new failures; 19 pre-existing failures unchanged), and running-app review via a release web build driven with Playwright across all three sports |
| 2026-09-01 | Shared Cyber UI search field | Input polish | BUILT | Reworked the shared search control as a 56 px rectangular panel, removed the inherited nested filled input and rounded outline, and retained one calm tokenized cyan border with no glow. | `design/cyber-ui-design-system.md` | `lib/widgets/cyber/cyber_widgets.dart`, `test/match_search_screen_test.dart`; targeted widget test and Flutter analyze |
| 2026-09-01 | Collections / motorsport player cards | Asset mapping | BUILT | Added 80 supplied labelled motorsport driver portraits and mapped each exact roster ID to its player card, while retaining the 22 existing F1 PNG portraits and fallback for the three unpictured drivers. The archive contained no repeated labels. | `systems/collections-decks-and-packs.md` | `assets/racing_driver_images/`, `lib/data/racing_portraits.dart`, `test/racing_portraits_test.dart`; targeted Flutter analyze, portrait bundle test, and production Flutter web build |
| 2026-09-01 | Penalty Shootout | Character design polish | BUILT | Rebuilt the live kick scene around a card-derived articulated kicker and goalkeeper matching Hoop Duel's procedural athlete language, while preserving the goal-facing aim zones, scoring rules, sounds, haptics, and payoff glow. Added run-up, strike, follow-through, dive, recovery, and outcome reactions for both sides. | `games/penalty-shootout.md` | `lib/screens/shootout/widgets/penalty_keeper_rig.dart`, `lib/screens/shootout/widgets/penalty_goal_frame.dart`, `lib/screens/shootout/widgets/shootout_phase.dart`, `test/shootout_phase_test.dart`; targeted Dart/Flutter analyze and shootout tests |
| 2026-09-03 | Hoop Duel / basketball player cards | Asset mapping | BUILT | Added 40 supplied labelled NBA portraits, each mapped to its exact current-roster player card. The archive had no duplicate labels, so first-occurrence handling was not needed. Existing mapped portraits and fallbacks are preserved. | `systems/collections-decks-and-packs.md` | `assets/basketball_player_images/`, `lib/data/basketball_portraits.dart`, `test/basketball_portraits_test.dart`; targeted Flutter analyze, portrait bundle test, and production Flutter web build |
| 2026-09-04 | Collections / cricket player cards | Asset mapping | BUILT | Added 35 supplied IPL portraits, matched by their exact player-card IDs and stored at the display-name asset paths used by the existing resolver. The archive had no repeated labels; existing portraits and fallbacks are preserved. | `systems/collections-decks-and-packs.md` | `assets/cricketer_images/`, `test/cricket_portraits_test.dart`; targeted Flutter analyze and portrait bundle test |
| 2026-09-04 | Predictions / basketball STATS | Data visualisation | BUILT | Rebuilt the basketball scoring map on a drawn NBA half court. The old painter mapped ESPN shot coordinates onto a full-court rectangle with the wrong divisors, squashing every shot into the left half; coordinates are now read in their true frame (`x` 0..50 across the baseline, `y` = feet out from the rim, origin at the rim) and drawn in feet against real court furniture. Twos read as filled dots and threes as hollow rings, with a team-colour legend under the court. | `systems/predictions.md` | `lib/screens/predictions/widgets/basketball_match_stats_view.dart`, `test/basketball_cricket_match_stats_view_test.dart`; targeted Flutter analyze, the basketball/cricket stats-view tests, and a rendered golden probe checked against `assets/data/basketball-revamp.json` shot distances |
| 2026-09-04 | Cross-sport identity / tabs / onboarding / Trending | Design-system consistency | BUILT | Established the canonical sport palette (Football cyan, Cricket white, Basketball yellow, Tennis green, Motorsport red) across sport tabs, onboarding/Profile selectors, Trending markers, All Sports, collections, shop, and leaderboards. Inactive tab icons retain subdued identity color while only the selected underline/selector glows; semantic LIVE, reward, team, rarity, and game-mode colors remain unchanged. | `design/cyber-ui-design-system.md`, `systems/profile-onboarding-and-settings.md`, `systems/predictions.md`, `README.md` | `lib/config/sport_modules.dart`, shared cyber sport/underline tabs, onboarding/Profile selectors, collection/shop/leaderboard filters; sport metadata, underline-tab, onboarding, collection, and deck widget tests; targeted Flutter analyze and Flutter web debug-server smoke (interactive browser review unavailable) |
| 2026-09-04 | Final Over / Games and Trending hero cards | Visual identity polish | BUILT | Changed the Final Over card treatment from cyan to Cricket's canonical white across its HUD frame, badge, subtitle, wicket, pitch outline, and delivery trail while preserving the green outfield and gold-brown pitch illustration. | `games/final-over.md` | `lib/screens/predictions/prediction_home_screen.dart`; Final Over Games-tab and game-hero widget tests; targeted Flutter analyze and Flutter web debug-server smoke |
| 2026-09-04 | Penalty Shootout and 5v5 Football Chess / Games and Trending hero cards | Visual identity polish | BUILT | Aligned both football game cards to the canonical cyan theme. Penalty Shootout now uses cyan chrome, net, target, and shot trail; Football Chess uses cyan chrome, board tint, outline, player pieces, and move trail with neutral white opponent pieces. Gameplay semantics and team colors are unchanged. | `games/penalty-shootout.md`, `games/football-chess.md` | `lib/screens/predictions/prediction_home_screen.dart`; game-hero widget tests; targeted Flutter analyze and Flutter web debug-server smoke |
| 2026-09-04 | Predictions / MATCH Trending cards | Visual consistency | BUILT | Standardized every Trending signal-card shell, including loading and unavailable states, on a cyan-tinted panel background with cyan chamfered border, top signal line, and hard-elevation edge. Semantic LIVE/FUTURE/PREDICT/PICK and sport-marker colors remain distinct inside the shared shell. | `systems/predictions.md` | `lib/screens/predictions/widgets/trending_match_bento.dart`; targeted Flutter analyze and Trending widget verification |

| 2026-09-05 | Friends / rival social actions | Gratification feedback | BUILT | Added a shared cyber transmission animation after adding a rival from either Friends Arena or Rival Dossier: the outbound signal travels, resolves to one confirmation tick with REQUEST SENT copy, and lands with confirm audio/haptics; removal keeps its calm snackbar feedback. This remains a local bookmark confirmation, not a remote request/acceptance backend. | `systems/friends.md` | `lib/screens/friends/widgets/friend_request_sent_animation.dart`, `lib/screens/friends/friends_arena_screen.dart`, `lib/screens/profile/rival_profile_screen.dart`; `test/friend_request_sent_animation_test.dart`, `test/friends_cubit_test.dart`, `test/referral_screen_test.dart`; targeted Flutter analyze and tests |
| 2026-09-05 | Hoop Duel / basketball player cards | Asset mapping | BUILT | Added 40 supplied labelled NBA portraits, each mapped to its exact current-roster player card. The archive had no repeated labels, so its first-occurrence policy required no conflict resolution. Existing mapped portraits and fallbacks are preserved. | `systems/collections-decks-and-packs.md` | `assets/basketball_player_images/`, `lib/data/basketball_portraits.dart`, `test/basketball_portraits_test.dart`; targeted Flutter analyze and portrait bundle test |
| 2026-09-05 | Hoop Duel / basketball and tennis player cards | Asset mapping | BUILT | Added eight supplied basketball portraits and 41 supplied tennis portraits from the 50-file archive. The two Diana Shnaider labels resolve to one card; the first archive entry is retained, as requested. Existing portraits and fallbacks are preserved. | `systems/collections-decks-and-packs.md` | `assets/basketball_player_images/`, `assets/tennis_player_images/`, portrait maps, and portrait tests; targeted Flutter analyze and bundle tests |

| 2026-09-05 | Hoop Duel / basketball player cards | Asset mapping | BUILT | Added 20 supplied labelled NBA portraits, each mapped to its exact current-roster player card. The archive had no repeated labels, so first-occurrence handling required no conflict resolution. Existing mapped portraits and fallbacks are preserved. | `systems/collections-decks-and-packs.md` | `assets/basketball_player_images/`, `lib/data/basketball_portraits.dart`, `test/basketball_portraits_test.dart`; targeted Flutter analyze and portrait bundle test |

## Ledger Rules

- Append new entries; do not rewrite historical rows to make later behavior
  appear older.
- Use the implementation date in `YYYY-MM-DD` form.
- Include the affected page or folder and the code/tests used to verify claims.
- A new game, feature, design rule, economy rule, navigation path, or planned
  scope change must update its page and this ledger in the same change.
- Update the main product index when coverage, navigation, or cross-feature
  behavior changes.
| 2026-09-05 | Daily Fulham vs Chelsea reference fixture | BUILT / PROTOTYPE DATA | Reuse the basketball/cricket local-date rebasing for the imported football match in catalog and detail lookup, preserving its completed report. | `systems/predictions.md` | `prediction_repository.dart`; football package rollover regression and basketball/cricket package tests; targeted Flutter analyze. |
| 2026-09-05 | Football and basketball fixture-card result summaries | BUILT | Removed the redundant winner-summary line from completed football and basketball cards; preserved cricket margin summaries. | `systems/predictions.md` | `match_prediction_card.dart`; targeted match-card widget test and Flutter analyze. |
| 2026-09-05 | Football and basketball fixture-card result hierarchy | BUILT | Completed decisive-score cards now show the winning team name and score in white, with the losing side muted; draws remain balanced. | `systems/predictions.md` | `match_prediction_card.dart`; result-tone widget test and targeted Flutter analyze. |
| 2026-09-05 | Football STATS event and commentary logs | Visual hierarchy | BUILT | Removed the individual filled, four-sided boxes from every event and commentary entry while retaining timeline rails, icons, timestamps, sequence labels, and readable spacing. | `systems/predictions.md` | `football_match_stats_view.dart`; football stats HUD widget test and targeted Flutter analyze. |
| 2026-09-05 | Football STATS overview entry point | Visual hierarchy | BUILT | Removed the redundant football match-pulse hero so OVERVIEW starts directly at MATCH INTEL beneath the persistent match header; cricket and basketball pulse heroes remain. | `systems/predictions.md` | `football_match_stats_view.dart`; football stats HUD widget test and targeted Flutter analyze. |
| 2026-09-05 | Predictions / football match data | Data enrichment | PROTOTYPE DATA | Added pitch shot positions to the bundled Fulham-Chelsea football package so the football report can plot a shot map like basketball's scoring map. Confirmed ESPN's soccer summary endpoint does expose per-shot coordinates (`fieldPositionX`/`fieldPositionY` on `commentary[].play`), which the package decoder had never captured, and decoded the frame empirically: x = distance from the attacking goal / 52.5m (box edge at 0.34), y = 0 at the attacker's left touchline to 1 at their right, both teams in the same attacking frame with no half-time flip. All 32 shot events in the package gained `fieldPositionX`, `fieldPositionY`, `shotZone` and a `positionSource` marker, split 14 home / 18 away to match the package's own `totalShots`. Because the fixture is a synthetic 2026-27 match with no tracked positions, each shot took a real Premier League coordinate sampled from a 2,655-shot / 117-match ESPN corpus, matched on commentary zone, outcome and body part and chosen deterministically from the play id; positions are zone-true, not tracked. JSON changes are purely additive - all pre-existing content is byte-identical. | `systems/predictions.md` | `assets/data/football-revamp.json`; `test/football_match_package_service_test.dart` passes green (including "bundled football package preserves every supplied data section"), verified additive-only against a pre-change copy and cross-checked home/away shot counts against `topStats.totalShots`. Pre-existing unrelated failures in `test/football_match_stats_view_test.dart` and `test/match_detail_screen_test.dart` reproduce identically on the unmodified JSON and were left untouched. |
| 2026-09-05 | Predictions / football match data | Data correction | BUILT | Replaced the zone-sampled shot positions added earlier the same day with real ESPN tracked coordinates. The bundled fixture turned out to be a genuine ESPN match (event `401879318`, Fulham 2-3 Chelsea at Craven Cottage) rather than a synthetic one - same date, venue, 104 commentary entries and identical play ids - so all 32 shot events were joined by `playId` to their true positions and `positionSource` is now `espn`. Also corrected the documented coordinate frame: the current 2026-27 feed uses 0-100 with x as progress toward the attacking goal (goal line 100, box edge 83.1) and the attacker's left as the HIGH y value, which is inverted and rescaled relative to the 2024-25 feed the first pass had calibrated on. Recorded that the frame is season-specific and must be recalibrated against commentary prose on any future import. | `systems/predictions.md` | `assets/data/football-revamp.json`; recalibrated against 571 shots across 22 completed 2026-27 EPL matches, then verified all 32 written coordinates fall inside their commentary-zone bands (0 out of band), home/away split 14/18 matches `topStats.totalShots`, and 5 goals match the 5 listed scorers; `test/football_match_package_service_test.dart` green. |
| 2026-09-05 | Predictions / football STATS | Data visualisation | BUILT | Added a SHOT MAP panel to the football report, giving football the coordinate map basketball already had. It sits inside the MOMENTUM sub-tab the way the scoring map sits inside basketball's FLOW, and wears the same chart-surface chrome and range switcher so the two sports read as one system. A new `FootballShot` model and decoder path lift the tracked positions off the commentary entries; a `PitchFrame`/`FootballShotMapPainter` pair draws a full pitch to real metre proportions (penalty areas, goal areas, penalty spots, dashed D arcs, centre circle, goal mouths) as the football analogue of the basketball `_CourtFrame`. Home attacks right and away attacks left, mirroring handled in one place in `FootballShot.pitchOffset` because the feed hands both sides the same attacking frame. Outcome is encoded by shape (goal, on target, off target, blocked) and team by colour, with goals the only glowing mark so one focal class survives per panel; ALL/1ST/2ND/GOALS filters, a staggered reveal, and tap-a-mark selection with haptics and a pulse ring revealing taker, minute, outcome, assist and zone. | `systems/predictions.md` | `lib/models/football_match_data.dart`, `lib/services/football_match_package_service.dart`, `lib/screens/predictions/widgets/football_shot_map.dart`, `lib/screens/predictions/widgets/football_match_stats_view.dart`; new `test/football_shot_map_test.dart` covers decode completeness against the package's own shot aggregate, the zone-to-coordinate frame, the opposite-goal mirroring, MOMENTUM-tab rendering and filter narrowing; targeted Flutter analyze clean. |
| 2026-09-05 | Predictions / football STATS | Data visualisation | BUILT | Added a goal-mouth diagram to the football shot map's selected-attempt detail, showing where each attempt finished in the net. The feed publishes no goal-mouth coordinate (`goalPosition*` is zero throughout), but its commentary prose carries the placement for all 12 attempts that reached the frame and a miss direction for all 8 that did not; the 12 blocked attempts have no placement and are left null rather than guessed, rendering an empty frame with a BLOCKED caption at constant panel height. Commentary names a corner without saying whose left, so the diagram adopts the broadcast convention of the viewer's and says so. xG/xGOT are absent from the feed and are omitted rather than estimated or stubbed. Posts, crossbar, net and ball were extracted out of the penalty shootout into a shared `paintGoalMouth`, which the shootout now delegates to, replacing one of three duplicate private implementations. | `systems/predictions.md` | `lib/widgets/cyber/goal_mouth.dart` (new), `lib/screens/shootout/widgets/penalty_goal_frame.dart`, `lib/models/football_match_data.dart`, `lib/services/football_match_package_service.dart`, `lib/screens/predictions/widgets/football_shot_map.dart`, `test/football_shot_map_test.dart`. The extraction was proven pixel-identical to the shootout original before switching (rendered static and mid-ripple frames byte-compared, 0 of 436,800 bytes differing). 8 shot-map tests pass covering placement coverage, corner-to-coordinate mapping and tap-to-reveal; all four shootout suites pass; targeted analyze clean. A rendered probe of all four states caught the miss marker painting outside its box (CustomPaint does not clip) — fixed with a clip plus retuned frame proportions. The pre-existing unrelated failure in `football_match_stats_view_test.dart` remains, reproducing identically on an unmodified tree. |
| 2026-09-05 | Basketball and cricket STATS overview entry point | Visual hierarchy | BUILT | Removed the summary headers and their display calculations above GAME INTEL / MATCH INTEL, matching football and bringing match intelligence into view immediately. | `systems/predictions.md` | `basketball_match_stats_view.dart`, `cricket_match_stats_view.dart`; targeted Flutter analyze clean; all 3 existing basketball/cricket stats widget tests pass. Running-app visual verification attempted but not completed in this session. |
| 2026-09-05 | Shop and Top search | Discovery and navigation | BUILT / PROTOTYPE DATA | Added dedicated global Shop search with existing purchase tiles and reward flows, plus username search across the local rival roster. Shared search chrome, full-catalogue matching, country aliases, result grouping, and preserved back-navigation state. | `systems/economy-shop-and-ledgers.md`, `systems/leaderboards-rivals-and-history.md`, `README.md` | Shop search catalogue/screen, user search screen, shared search and rival widgets; 32 targeted tests pass including purchases, insufficient funds, navigation and 320px keyboard layouts. Web app compiles and serves; live browser inspection unavailable because no browser is connected. |
| 2026-09-05 | Basketball, tennis, football, and motorsport player cards | Asset mapping | BUILT | Added all 22 supplied labelled WebP portraits to their exact current-roster cards: 3 basketball, 12 tennis, 4 football, and 3 motorsport. Archive labels were unique, so first-occurrence handling required no conflict resolution. | `systems/collections-decks-and-packs.md` | Sport portrait asset folders, portrait maps, `cards.dart`; targeted portrait mapping and bundle tests plus Flutter analyze. |
| 2026-09-06 | Shop and Top search entry point | Navigation hierarchy | BUILT / PROTOTYPE DATA | Moved Shop and Top search from the persistent header into the right-hand action cell of their sport strips, matching Sports. The shared sport-strip component keeps the selected tab as the only persistent glow. | `systems/economy-shop-and-ledgers.md`, `systems/leaderboards-rivals-and-history.md` | `sport_underline_tabs.dart`, Shop and leaderboard screens; targeted widget tests and Flutter analyze. |
| 2026-09-06 | Cross-app team identity | Design system and palette data | BUILT | Adopted the supplied 6,248-record competition-scoped team palette as the authoritative logo source, preserving its three logo roles exactly and adding a deterministic hue-related `secondaryTextColor` that clears WCAG AA on all standard dark surfaces. Match-aware team UI now resolves exact competition aliases and uses the accessible role without adding persistent glow; semantic status and reward colors remain dominant. | `design/cyber-ui-design-system.md`, `systems/predictions.md`, `README.md` | `tool/generate_team_palettes.dart`, compact palette source and generated Dart data, `team_palettes.dart`, `TeamLogo`, match/search/prediction/stats/profile identity surfaces; generator check mode, palette and widget tests, targeted Flutter analyze, and successful local Flutter web compile/serve. Phone-width browser inspection was unavailable because no browser surface was connected. |
| 2026-09-06 | Predictions match feed / onboarding favorites | Feature implementation | BUILT | Made the onboarding club pick drive the match feed, where it previously did nothing outside the Profile crest strip. On any day a followed club plays, that fixture is lifted out of its league group and pinned under a YOUR CLUB header directly beneath the day navigator, with the rest of the day following below; the pin is day-scoped so it can never contradict the selected day. One fixture is pinned at a time (live, then next kickoff, then most recently finished), and when two followed clubs play in the same window a crest switcher above the pin chooses between them. The pinned fixture is removed from its league group so it is never shown twice. Every other followed-club fixture is partitioned to the front of its league group and that league to the front of the day, deliberately before the three-per-league preview cap so a club's game can never hide behind VIEW MORE. `MatchPredictionCard` gained an optional `favorite` so the marker travels to match search, league and team pages too: a flat YOUR CLUB plate on the top-left shoulder, the club's name in its identity color, and the club color on the resting border only (live and reward-pending colors keep priority; the marker never glows). Trending tiles show YOUR CLUB in the corner tag unless LIVE. Clubs are matched by identity rather than raw id — EPL/IPL fixtures arrive from live ESPN enrichment with ESPN's numeric ids and naming, so favorites resolve through an alias table and are scoped by sport, not league id. A player who follows no club sees the feed exactly as before. | `systems/predictions.md`, `systems/profile-onboarding-and-settings.md`, `README.md` | `lib/data/favorite_team_matcher.dart` (new), `lib/blocs/prediction/prediction_state.dart`, `lib/blocs/prediction/prediction_cubit.dart` (`refreshFollowing`), `lib/app.dart`, `lib/screens/profile/profile_screen.dart`, `lib/screens/predictions/prediction_home_screen.dart`, `match_prediction_card.dart`, `trending_match_bento.dart`, `match_search_screen.dart`, `team_detail_screen.dart`, `league_detail_screen.dart`; new `test/favorite_club_pin_test.dart` (8 tests: ESPN-shaped identity matching, sport scoping, pin hoisting, no-duplicate, marker reach, and a no-favorite regression) passes; `flutter analyze` clean on all changed files. The one failure in `test/prediction_home_day_navigation_test.dart` ("league match list caps at 3 games", expecting a `VIEW MORE // +2 GAMES` label the committed screen does not render) is pre-existing and reproduces identically on a stashed tree. |
| 2026-09-06 | Cricket STATS overview hierarchy | Visual hierarchy | BUILT | Removed the redundant INNINGS GRID from the Cricket OVERVIEW; innings detail remains available in RACE, CHASE, and SCORECARD. | `systems/predictions.md` | `cricket_match_stats_view.dart`; cricket/basketball stats widget test and targeted Flutter analyze. |
| 2026-09-06 | Cricket STATS RACE | Team identity | BUILT | Wicket-fall diamonds now use the batting team's resolved identity colour, so each innings reads as its own team on the shared scoring worm. | `systems/predictions.md` | `cricket_match_stats_view.dart`, `basketball_cricket_match_stats_view_test.dart`; targeted Flutter analyze and widget test. |
| 2026-09-06 | Cricket STATS RACE hierarchy | Visual hierarchy | BUILT | Removed the redundant RACE VERDICT panel; the chart legend remains the live source for both innings scores. | `systems/predictions.md` | `cricket_match_stats_view.dart`, `basketball_cricket_match_stats_view_test.dart`; targeted Flutter analyze and widget test. |
| 2026-09-06 | Cricket fixture-card result hierarchy | Visual hierarchy | BUILT / PROTOTYPE DATA | Completed cricket cards now place each innings score beside its team crest, keep overs and target context below that team's name, and use white/muted winner and loser tones. | `systems/predictions.md` | `match_prediction_card.dart`, `match_detail_screen_test.dart`; targeted Flutter analyze and widget test. |
| 2026-09-06 | Cricket STATS navigation | Navigation hierarchy | BUILT | Removed the SQUADS tab and its unreachable squad panel from the Cricket report, leaving overview, race, chase, scorecard, and match feed. | `systems/predictions.md` | `cricket_match_stats_view.dart`, `basketball_cricket_match_stats_view_test.dart`; targeted Flutter analyze and widget test. |
| 2026-09-06 | Predictions match feed / club marker | Card design polish | BUILT | Removed the YOUR CLUB badge plate from the fixture card at the player's request. The card is dense and the plate competed with the centred status tag for the same top edge; the club is still marked on every card by its name in the club identity color plus the club-tinted resting border, and the pin header above the fixture still names the club in full. The Trending tile keeps its YOUR CLUB corner tag, which has its own free slot and no such collision. | `systems/predictions.md` | `lib/screens/predictions/widgets/match_prediction_card.dart` (dropped `_YourClubPlate` and its `_PlateClipper`); `test/favorite_club_pin_test.dart` updated to assert the card carries no badge while the pin header still names the club, 8 tests pass; targeted `flutter analyze` clean; confirmed in the running app via a release web build and a temporary seeded entrypoint — both the pinned card and the in-league club card render badge-free with the club-coloured name and border intact. |
| 2026-09-06 | Cricket STATS innings run-rate graph | Navigation and data visualisation | BUILT / PROTOTYPE DATA | Removed the standalone CHASE tab and its late-chase readout, then added a glow-free INNINGS RUN RATE panel beneath INNINGS RACE with a 1ST/2ND INNINGS switch. It plots actual run rate in the batting team colour, required rate in magenta only for the chase, and every four/six as a gold value-positioned labelled marker; close labels stagger while their plotted anchors remain exact. Added complete local legal-delivery progression for both innings with decoder integrity checks and contextual unavailable handling. | `systems/predictions.md`, `README.md` | `cricket-revamp.json`, cricket data models/package decoder, `cricket_match_stats_view.dart`, shared `cyber_chart.dart`; decoder/widget/chart tests and targeted Flutter analyze. |
| 2026-09-06 | Match-page team-name hierarchy | Visual hierarchy | BUILT | Set both team names in the persistent match summary header to white. Team identity remains on the supplied crests and split score rail, keeping names legible without adding persistent colour or glow. | `systems/predictions.md` | `match_summary_header.dart`, `match_summary_header_test.dart`; targeted Flutter analyze, widget test, and successful web build. |
| 2026-09-06 | Cricket MATCH FEED commentary hierarchy | Navigation and visual hierarchy | BUILT / PROTOTYPE DATA | Removed the Notes mode from Cricket MATCH FEED. Commentary is now split by batting team, and each ball renders as an open timeline row with a semantic rail instead of a bordered card. The bundled prototype currently publishes RCB commentary only, so GT exposes an innings-specific unavailable state. | `systems/predictions.md` | `cricket_match_stats_view.dart`, `basketball_cricket_match_stats_view_test.dart`; targeted Flutter analyze and widget test. |
| 2026-09-06 | Whole app / Cyber UI | Background visual treatment | BUILT | Removed the shared procedural film-grain/noise layer from every screen, including Penalty Shootout; retained the blueprint grid, scanlines, and vignette so the HUD remains deliberate and readable without speckling. | `design/cyber-ui-design-system.md` | `cyber_widgets.dart`, `game_scaffold.dart`, all former in-match grain call sites; targeted Flutter analyze and production web background render |
| 2026-09-06 | Predictions / league hub header | Visual hierarchy | BUILT | Simplified the league lockup subtitle to its season token only (for example, `2026-27`), removing the duplicated league name and team count beneath the already-prominent competition title. | `systems/predictions.md` | `league_detail_screen.dart`; targeted Dart analyzer clean and source-level subtitle verification |
| 2026-09-06 | Predictions / basketball match STATS → FLOW | Feature replacement | BUILT / PROTOTYPE DATA | Removed win probability from the basketball match entirely and replaced its chart with a two-team SCORING RUN race. The `WIN PROBABILITY` panel (two mirrored probability traces on a locked 0-100 percent axis) became a stepped score race built on the same shared `CyberChartPanel` used by cricket's INNINGS RACE, so drag-to-scrub, the dashed playhead, the GAME / H1 / H2 / CLUTCH range tabs, the live legend and expand-to-fullscreen all carry over unchanged: home filled, away stroked, a real point-total y-axis (`yAxisLabels`, 4 grid divisions) instead of percentages, `Q1`-`Q4` x-axis labels sampled from the actual play at each label fraction, and lead-change rings in the colour of the team that took the lead — capped at the closing twelve so a see-saw game cannot draw a full-height cage, with only the final flip focal to keep glow scarce. Added a SCORING EDGE strip (BIGGEST LEAD in the leading team's colour, LEAD CHANGES, TIES) on the shared `CyberMiniMetric`, derived in one pass by a new `_ScoringEdge.from`, so the graph lands on earned numbers rather than ending flat. Stripped the remaining win-% readouts: TURNING POINTS rows now accent on the point margin and carry a `CyberStatPill` reading the leading team's code plus margin in that team's colour (`NY +3`) — a `CyberDeltaChip` was tried first in running-app review and rejected, because a red ▼3 PTS reads as "something dropped" rather than "NY lead by 3" — with a scoreline subtitle matching the PLAYS rows, and the PLAYS rows dropped their `HOME 63.4%` suffix. `_turningPointMarkers` was replaced by `_leadChangeMarkers` + `_periodLabels`. No model or service change — `homeWinPercentage` / `swing` stay parsed and stored, just never rendered. | `systems/predictions.md` | `basketball_match_stats_view.dart`; `basketball_cricket_match_stats_view_test.dart` (chart key, SCORING RUN / SCORING EDGE, and a `findsNothing` guard on any `%` text in FLOW); clean `flutter analyze` on both changed files, and `flutter test basketball_cricket_match_stats_view_test.dart cyber_chart_test.dart basketball_cricket_match_package_service_test.dart` all passing |
| 2026-09-06 | Predictions / league hub data layer | Data extraction | BUILT (data) / PLANNED (STATS page) | Extracted every stat ESPN publishes for the Premier League (`eng.1`) and LaLiga (`esp.1`) into one bundled package, `assets/data/football-league-stats.json` (299.8 KB, compact), ahead of building a league hub STATS tab. Per league: the 20-team table with all 14 standings fields and qualification-zone notes, all 12 leader categories 25 players deep with every athlete reference resolved to a name/jersey/position/nationality (131 EPL, 140 LaLiga unique athletes), and per-team season statistics from a core-API feed the app had never touched — 112 stats per team across `offensive`/`defensive`/`general`/`goalKeeping`, including xG, possession, clean sheets, tackle and duel win rates. Two size decisions kept the asset at 300 KB instead of 1.07 MB: stat metadata (display names plus ESPN descriptions) is hoisted into one shared `statDictionary` rather than repeated per team, and each stat reduces to a `[value, displayValue]` pair; athletes are deduped into an id-keyed map because the same striker tops several boards. Each league carries an `aliases` list because the app has three colliding league-id namespaces. No `lib/` change: nothing reads the asset yet, and the pre-existing `epl` vs `eng.1` slug mismatch and missing `esp.1` registration in `EspnLeagueStatsService` are recorded as outstanding rather than fixed here. The field inventory records per-stat coverage (how many of 20 teams report a non-zero value), flags 22 stats that are zero league-wide, and warns that ESPN reports `teamStats.general.draws` and `minutes` as 0 even when the standings disagree, so W/D/L must come from `standings[].stats`. | `data/league-stats-field-inventory.md` (new), `systems/predictions.md`, `README.md` | `tool/generate_league_stats.dart` (new); clean `flutter analyze` on the generator; full extraction run plus a 29-assertion structural and referential-integrity pass (20 teams / 20 standings rows / 12 categories x 25 rows / 112 stats x 20 teams per league, every `athleteId` and `teamId` resolving, ranks 1-20 in order); `--check` verified clean immediately after write to prove determinism; spot-checked EPL and LaLiga tables and top scorers, and cross-checked the `draws`/`minutes` anomaly against the raw ESPN response |
| 2026-09-06 | Predictions / league hub TABLE, LEADERS, STATS | Feature build | BUILT | Wired the bundled `assets/data/football-league-stats.json` package into the league hub and added a fifth **STATS** tab. Fixed the reason the hub was empty: the same competition reaches `LeagueStatsCubit` under four different identifiers (curated `eng.1`, follow-list `epl`, ESPN scoreboard `700`, ESPN standings `23` — the two ESPN numbers differ), and the slug map was keyed only `epl`, so `_slugs[league.id]` missed and the repository standings fallback (also keyed `epl`) missed too, rendering `0 TEAMS` with NO TABLE YET / NO STAT LEADERS. Leagues now resolve by normalised alias with league name and short code as fallbacks, and the live `EspnLeagueStatsService` slug map gained every spelling plus `esp.1`. Loading is bundled-first: the package renders immediately and the live ESPN feed is layered over it via `LeagueStatsSnapshot.mergedWith`, which keeps package-only team stats and ignores an empty live result, so a failed or CORS-blocked request can never downgrade a filled hub. LaLiga was previously unreachable (no fixtures, so no league strip), so `esp.1` was added to the football scoreboard sweep. The new STATS tab presents four category tabs (ATTACK / DEFENCE / KEEPING / DISCIPLINE) over a league-total pulse strip, a `CyberFilterChips` stat selector and a `TeamStatBoard` ranking all 20 clubs — deliberately the team-side twin of the LEADERS player boards (same champion-plus-chasers rhythm, one focal glow), with accents following existing colour discipline (league accent / violet / success / amber, red cards in danger). Boards where the smallest number wins rank ascending and read `LEAGUE BEST // FEWEST`; their bars scale against the board peak so the leader no longer drew a full bar while an identical value one row below drew a short one. Only stats verified populated in both competitions get a board (22 of 112 are zero league-wide); captions come from ESPN’s stat dictionary, overridden for three entries where the feed is ambiguous (`totalGoals` reads as goals conceded) or misspelt (`tacklePct`, `foulsCommitted`). | `systems/predictions.md`, `data/league-stats-field-inventory.md` | `league_stats_package_service.dart` (new), `team_stat_board.dart` (new), `league_detail_screen.dart`, `league_stats_cubit.dart`, `league_stats_state.dart`, `league_stat_leaders.dart`, `espn_league_stats_service.dart`, `espn_score_service.dart`, `app.dart`; `test/league_stats_package_service_test.dart` (new, 4 tests: package fills both leagues at 20 teams / 12 boards / 112 stats, all six id spellings plus name fallback resolve, every curated board ranks 20 clubs in the right direction, live merge preserves team stats); clean `flutter analyze` on all changed files; full `flutter test` failure set byte-identical to the pre-change baseline (16 named pre-existing failures, no regressions); verified in the running release web build driven by Playwright — TABLE, LEADERS and all four STATS groups screenshotted and read, with live ESPN blocked by CORS throughout, proving the bundled-first path renders the hub unaided |
| 2026-09-06 | Predictions / league hub STATS live source | Robustness fix | BUILT | Added a live ESPN fallback for the league hub STATS tab, which showed NO CLUB STATS on any install where the bundled `football-league-stats.json` package was unavailable (for example an app build predating the asset): TABLE and LEADERS filled from the live feed, so the header read `20 TEAMS`, while STATS stayed empty because `EspnLeagueStatsService` never fetched per-team season statistics at all. It now does, from `.../seasons/{year}/types/1/teams/{id}/statistics`, one request per club at six concurrent, building the same `TeamSeasonStats` + stat-dictionary models the package produces so the tab renders identically from either source. The sweep is lazy — triggered from the STATS tab first build, never on hub load, and skipped entirely when the package already answered — matching the existing lazy athlete-name resolution on LEADERS; results are cached per league for the app lifetime. New `LeagueTeamStats` container, `LeagueStatsSnapshot.withTeamStats`, and a `loadingTeamStats` state flag driving a PULLING CLUB STATS loader so the tab no longer flashes its empty state while fetching. `mergedWith` now prefers live team stats when present instead of always keeping the package copy. The empty-state copy no longer claims coverage is limited to two leagues, since STATS now works for any competition in the slug map. | `systems/predictions.md` | `espn_league_stats_service.dart`, `league_stat_leaders.dart`, `league_stats_cubit.dart`, `league_stats_state.dart`, `league_detail_screen.dart`; `test/espn_league_team_stats_live_test.dart` (new, skipped by default) run against the real API and passing in 15s — 20 clubs and 100+ stats for both eng.1 and esp.1, with every curated board ranking all 20 clubs off live data; clean `flutter analyze`; existing `league_stats_package_service_test.dart` still green; full `flutter test` failure set byte-identical to the pre-change baseline (16 pre-existing, no regressions); bundled path re-verified in the release web build after the refactor |
| 2026-09-06 | Cricket STATS / SCORECARD | Visual hierarchy and interaction | BUILT | Rebuilt the innings scorecard as a cyber HUD: cut-corner innings controls, a compact score command panel, tabular batting and bowling tables, open section rails, structured innings telemetry, a short switch transition, and selection haptics. Cyan is reserved for the active innings and score, gold marks the leading run and wicket figures, and persistent table surfaces remain glow-free. | `systems/predictions.md` | `lib/widgets/cricket_scorecard_view.dart`, `test/basketball_cricket_match_stats_view_test.dart`; targeted Flutter analyze and widget tests |
| 2026-09-06 | Predictions / league hub LEADERS + STATS rows | Visual simplification | BUILT | Removed the per-row progress meter from both stat boards: `StatLeaderboard` (player leaderboards on LEADERS) and `TeamStatBoard` (club boards on STATS). Both carry short values already right-aligned in a tabular column, so the bar behind each row added weight without adding information — rank, crest, name and number carry the comparison on their own. Chaser rows collapse from a two-line Column (name over bar) to a single centred name row, making the lists denser; the leader plates keep their glow and oversized number as the single focal element, and their explainer/detail line moves up into the space the bar occupied. Removed the now-dead scaling machinery with it: the `fraction` parameter on all four row/plate widgets, the `top` and `peak` computations, and the `StatLeaderCategory.topValue` model getter, which had no remaining callers. | `systems/predictions.md` | `stat_leaderboard.dart`, `team_stat_board.dart`, `league_stat_leaders.dart`; clean `flutter analyze`; `league_stats_package_service_test.dart` still green; both tabs re-verified by screenshot in the release web build; full `flutter test` shows no regressions — the four entries not in the earlier baseline were re-checked individually, three (`match_detail_screen_test`) reproduce with the changes stashed and are wall-clock dependent, and `football_match_stats_view_test` passes in isolation as a pre-existing full-suite flake |
| 2026-09-06 | Predictions / football LINEUPS player dossier | Feature build | BUILT | Made every player on the football LINEUPS pitch tappable and gave each one a MATCH DOSSIER carrying that player's stats **for that match**, a touch heatmap and a player image, sourced from ESPN. Live probing settled what is actually obtainable (event `401879318`): the summary response the lineup already fetches carries a complete 14-stat sheet for all 40 players plus `subbedIn`/`subbedOut`/`formationPlace`, and `EspnScoreService._parseLineup` was throwing every bit of it away; the core plays feed carries 1,577 plays of which 974 have pitch coordinates and 989 name an athlete, intersecting to **1,521 attributed touches across 31 players** (all 22 starters >= 35, the 9 unused subs correctly none) plus per-shot `expectedGoals`. Sources split by cost: the free stat sheet is now parsed on the **live** path via a new extracted `parseSoccerRosterLineup`, so every football fixture gets a match sheet, while the ~1.5 MB plays feed is baked into the bundled asset only (live fixtures show a 'no tracked touches' state and keep the full sheet). Two pre-existing live-path bugs fixed in the same pass, called out rather than slipped in: `_parseLineup` dropped `formationPlace`, which the pitch board sorts by, so **every live match's lineup was laid out in arbitrary roster order**; and the football summary URL hardcoded `soccer/fifa.world` for every fixture regardless of competition, now resolved by alias with `fifa.world` only as fallback. The dossier is a squad-paging bottom sheet: identity plate, a count-up impact strip of TOUCHES / xG / FINAL 3RD / TERRITORY (all four derived from the coordinates, none published by ESPN), the heatmap with a FULL/1ST/2ND filter, and ESPN's numbers as grouped pills with inapplicable stats omitted rather than zeroed. Pitch nodes gained earned-glyph badges (goal, assist, card, substitution) with only the goal glowing. The heatmap uses additive radial falloffs at `BlendMode.plus` inside one `saveLayer` over a bilinear-splatted, twice-blurred 24x16 grid — no blur filter, no offscreen bitmap, no new dependency — on the existing true-scale `PitchFrame`, extracted from the shot map into a shared `paintFootballPitch`. Its ramp is fixed (cyan->lime->amber->white) rather than team-keyed after screenshots showed Fulham's white accent collapsing both ends of the ramp and washing the map out. **ESPN publishes no headshots for soccer** (three URL patterns, all 404) and the bundled portrait library covers 2 of these 40 players, so the card falls back to ESPN's per-event kit render (1440px, decoded downscaled) over a permanent shirt-number badge, so a slow or failed image never leaves a blank plate. Asset grew 82.7 KB -> 96.1 KB; the generator merges and never rewrites, asserting every hand-authored subtree byte-identical before writing, and keeps the file pretty-printed (unlike the league-stats asset) because its commentary prose is hand-authored and must stay editable. | `data/football-match-player-field-inventory.md` (new), `systems/predictions.md` | `tool/generate_football_match_players.dart` (new), `tool/espn_feed_client.dart` (new), `espn_soccer_lineup_parser.dart` (new), `football_player_heatmap.dart` (new), `football_player_match_sheet.dart` (new), `football_match_portraits.dart` (new), `football_match_data.dart`, `sport_match.dart`, `football_match_package_service.dart`, `espn_score_service.dart`, `match_pitch_view.dart`, `football_shot_map.dart`, `cyber_widgets.dart`; `test/football_player_match_stats_test.dart` (new, 9 tests asserting the real feed numbers — 1,521 touches, 31 tracked players, 22 starters >= 35, 9 unused subs empty, every touch clamped inside 0..1, every shipped stat key resolving in the label catalogue, and goals cross-checked against the asset's independently hand-authored scorer list), `test/football_player_heatmap_test.dart` (new, 9 tests: centre-touch symmetry about both axes, normalisation, empty-grid degradation, mirroring, and a refactor lock proving `FootballShot.pitchOffset` is unchanged by the extraction), `test/espn_soccer_lineup_parser_test.dart` (new, 10 tests against a trimmed checked-in fixture, including the `formationPlace` regression and every colliding league-id spelling), `test/football_match_stats_view_test.dart` (extended with 3 interaction tests; its original assertions kept verbatim); clean `flutter analyze` across `lib/`, `tool/` and `test/`; `--check` clean immediately after write proving determinism; verified in the release web build under Playwright — badged pitch, an outfield card, and the keeper's card (own-half hot blob, TERRITORY 12%, BOX 0, xG omitted) all screenshotted and read, and the kit renders confirmed to load (they are not CORS-blocked, unlike ESPN's JSON APIs) |
| 2026-09-07 | Predictions / football LINEUPS pitch surface | Visual simplification | BUILT | Removed the decorative 32px blueprint grid from the football lineup pitch in `_PitchSurfacePainter`. The pitch panel already sits on the app's textured background, so a second grid drawn inside it was texture on texture, and it competed for attention with both the real pitch markings and the player nodes — which now carry earned goal/assist/card/substitution badges and are the point of the board. The surface keeps only genuine markings: outline, halfway line, centre circle, penalty boxes and goal boxes. Scoped to football deliberately: the painter is shared with the basketball lineup board, which keeps its grid until that board is reviewed on its own, so the change is gated on the existing `basketball` flag rather than deleted outright. | `systems/predictions.md` | `lib/widgets/match_pitch_view.dart`; clean `flutter analyze`; `football_match_stats_view_test.dart` and `basketball_cricket_match_stats_view_test.dart` green (the one failure in the run, `match_circle_widget_integration_test`, is a pre-existing audio-backend failure that reproduces with the changes stashed); both Fulham and Chelsea boards re-screenshotted in the release web build and read |
| 2026-09-07 | Predictions / league LEADERS player season dossier | Feature build and ESPN data integration | BUILT / LIVE ESPN DATA | Made every league-leader plate open a player season dossier. The view uses the existing bundled leader identity immediately, then lazily combines ESPN core athlete identity and the exact `types/1/athletes/{id}/statistics` season split, keyed to the snapshot's numeric season. It presents only verified fields (bio, nationality, shirt, status and ATTACK / DEFENCE / GENERAL / KEEPING stats), deliberately omits unsupported contract/transfer/preferred-foot fields and unreliable soccer headshots, and shows a cached identity state when web CORS or the feed prevents the live scan. The player-dossier hero is the single live glow; all detailed data stays in flat cut-corner HUD surfaces with category selection and a complete-sheet reveal. | `systems/predictions.md`, `README.md` | `football_player_profile.dart`, `espn_football_player_profile_service.dart`, `football_player_profile_screen.dart`, `league_detail_screen.dart`, `stat_leaderboard.dart`, `league_stat_leaders.dart`; targeted Flutter analyze plus `league_stats_package_service_test.dart` and `football_player_profile_screen_test.dart` passing |
| 2026-09-07 | Predictions / player dossier SCOUTING REPORT | Visual hierarchy | BUILT | Removed the enclosing report panel so the existing cut-corner stat tiles and complete-sheet action sit directly on the page; this removes redundant framing while retaining the dossier's single glowing identity hero. | `systems/predictions.md` | `football_player_profile_screen.dart`; targeted Flutter analyze and player-dossier widget test |
| 2026-09-07 | Predictions / football EVENTS halftime transition | Visual hierarchy | BUILT / PROTOTYPE DATA | Suppressed the duplicate `START 2ND HALF` feed marker during football timeline decoding. EVENTS now retains a single semantic `HALFTIME 1 - 2` rail at the interval. | `systems/predictions.md` | `football_match_package_service.dart`, `football_match_package_service_test.dart`; targeted Flutter analyze and package test |
| 2026-09-07 | Predictions / cricket SCORECARD player dossier (IPL) | Feature build | BUILT | Mirrored the football player dossier for cricket: every batting and bowling row on the SCORECARD now opens a MATCH DOSSIER with that player's stats for that match, an innings tape and a portrait. The bundled fixture turned out to be the real IPL 2026 final (ESPN league 8048, event 1535465) with **all 24 player ids matching ESPN exactly** and every hand-transcribed batting and bowling figure reconciling with the API with zero disagreements, so the new layer is purely additive. **ESPN publishes NO positional data for cricket** — no wagon wheel, no pitch map, no line and length, and the core plays feed that gives football its coordinates returns HTTP 400 for the sport — so the hero visual is an INNINGS TAPE (the sequence of deliveries) rather than a heatmap. That is a data constraint, and it is documented so nobody hunts for a wagon wheel again. What ESPN does publish: a **46-stat sheet for all 24 players** (three times football's 15, spanning batting, bowling and fielding) nested under `rosters[].roster[].linescores[].linescores[].statistics` — a very different shape from soccer's flat array, and one the app was discarding — plus **all 233 deliveries** with both batsman and bowler named. Rebuilding every batter's innings from the ball feed reproduces the stat sheet exactly for all 12 batters once wides are excluded from balls faced, and also matches the hand-transcribed scorecard: a three-way agreement the generator asserts on every run. Sources split by cost — the free stat sheet is parsed on the LIVE path via a new extracted `parseCricketRosterSquad`, so any cricket fixture gets a card, while the ~64 KB ball feed is bundled only. Live also gained the `CricketBatter.id`/`CricketBowler.id` the rows join on, which the live parser had been dropping. The generator **splices text and re-encodes nothing**: an earlier decode-merge-re-encode reflowed thousands of lines because the hand-transcribed asset mixes formatting styles, so the generated block now lives at the root keyed by athlete id and the diff is 346 added lines against 1 removed. It refuses to run twice over the same file, and gained `--summary`/`--pbp` flags for reproducible offline runs. Two visual fixes came out of screenshots: metric labels were truncating, and a good economy rendered red because RCB's accent IS red — so the tape and spell colours are now semantic rather than team-keyed, the same lesson as the football heat ramp. Also fixed `cricketPortraitAssetForName`, which could not reach 20 of the 180 bundled portraits because they are named `10_Tim_David.webp` or `Bhuvneshwar_Kumar.webp` while the resolver lowercases — rescuing all 20 app-wide and lifting this squad from 18 to 22 of 24. The football sheet was refactored onto a new shared `PlayerMatchSheetScaffold` + `CountUpMetric` + `TapPunch` so there is one dossier shell, not two. | `data/ipl-match-player-field-inventory.md` (new), `systems/predictions.md` | `tool/generate_cricket_match_players.dart` (new), `espn_cricket_roster_parser.dart` (new), `cricket_innings_tape.dart` (new), `cricket_player_match_sheet.dart` (new), `cricket_match_portraits.dart` (new), `widgets/cyber/player_match_sheet.dart` (new), `cricket_match_data.dart`, `cricket_match_package_service.dart`, `cricket_scorecard_view.dart`, `cricket_match_stats_view.dart`, `sport_match.dart`, `cards.dart`, `espn_score_service.dart`, `football_player_match_sheet.dart`; `test/cricket_player_match_stats_test.dart` (new, 9 tests including the three-way reconciliation, the 233/124/109 delivery split, the wide-vs-leg-bye rule and phase splits partitioning the innings), `test/espn_cricket_roster_parser_test.dart` (new, 8 tests against a trimmed checked-in fixture — the nested linescores walk, keeper-from-position, and that the live path carries a sheet but no ball-by-ball), `test/basketball_cricket_match_stats_view_test.dart` (extended with 3 interaction tests; its existing assertions kept verbatim, including SQUADS/CHASE/INNINGS GRID still findsNothing); `basketball_cricket_match_package_service_test.dart` and `cricket_portraits_test.dart` unchanged and green, proving the merge was additive and the resolver only gained fallbacks; clean `flutter analyze` on all changed files; `--check` clean immediately after write; verified in the release web build under Playwright — a batter's card (Sundar 50* (37), 37-ball tape, phase splits) and a bowler's (Rasikh Salam 3/27, four spell bars) screenshotted and read. NOTE: a full-suite baseline comparison was not run because another session was concurrently mid-edit in `league_detail_screen.dart` and `espn_league_stats_service.dart`, leaving unrelated compile errors in the tree; all 31 cricket tests pass. |
| 2026-09-07 | Predictions / Profile Following | League season selector and follow control | BUILT / LIVE ESPN DATA | Added a route-local season selector and persistent FOLLOW/FOLLOWING control to ESPN-backed football league hubs. The selected year drives TABLE, LEADERS, STATS, GAMES, PICKS and player dossiers; historical PICKS is a read-only final-score archive with no odds or wallet path. Full-season schedules combine two calendar feeds, filter ESPN's embedded season and deduplicate event IDs, while web reports archive unavailability without blanking the bundled current campaign. Follow aliases persist as one canonical identity, following never assigns a club, unfollow confirms before removing an existing favourite, and Profile renders league-only follows as NO CLUB chips. | `systems/predictions.md`, `systems/profile-onboarding-and-settings.md`, `README.md` | `league_detail_screen.dart`, `standings_table.dart`, `league_stats_cubit.dart`, `league_stats_state.dart`, `league_stat_leaders.dart`, `espn_league_stats_service.dart`, `espn_score_service.dart`, `league_stats_package_service.dart`, `followable_leagues.dart`, `prediction_cubit.dart`, `profile_screen.dart`, `assets/data/football-league-seasons.json`; `league_stats_package_service_test.dart`, `league_stats_cubit_season_test.dart`, `league_follow_test.dart`, `league_header_controls_test.dart`; targeted Flutter analyze/tests and running-app visual check |
| 2026-09-07 | Predictions / IPL league hub TABLE, LEADERS, STATS | Feature build and ESPN data integration | BUILT | Built the IPL hub, which opened on a six-team mock table with NO STAT LEADERS and NO CLUB STATS. It now shows the real 2026 season: the ten-team table with net run rate and a playoffs cut line, nine leader boards, and four STATS board groups. **Cricket cannot use the football data path at all.** ESPN's core API rejects the sport outright (`Invalid sport/league combination (cricket/8048)`), so the season `leaders` feed and the per-team `statistics` feed that the football hub is built on do not exist, and `site.api.espn.com/.../8048/statistics` returns 403. Standings is the only league-level cricket feed that works. So LEADERS and STATS are **aggregated offline from every match summary of the season**: a new generator walks the 62-date calendar for event ids, fetches all 74 completed summaries, and sums the 46-stat player sheets into per-player and per-team season totals -> `assets/data/cricket-league-stats.json` (62.7 KB). That makes the package the hub's only source, not a first-render optimisation, so the cubit returns terminally on the cricket branch rather than layering a live refresh. Four things cost real time and are now documented: a completed cricket match has **no `completed` boolean** (it is `status.type.state == 'post'`, and filtering on `completed` silently returns zero matches); rates must be **recomputed from season totals, never averaged**, or a 4-ball cameo outweighs a 60-ball innings; rate boards need a **qualifying minimum** (100 balls faced, 120 bowled) or a single tidy over tops the economy chart; and `qualified` arrives as the numeric 1 on the four playoff teams, with 'Y' only in its displayValue. The generator **refuses a partial season** — an early run lost one match to a 502 and silently aggregated 73 of 74, which would have understated whoever played in it with nothing downstream able to tell. It runs at concurrency 2 with a `--delay` because ESPN rate-limits an IP hard (403 across all sports for tens of minutes). Three fixes came out of screenshots: the table rendered football's P/W/D/L/GD with net run rate wrapping onto two lines, because `StandingsTable` already has a cricket layout that it selects when `drawn` is null and the decoder was passing 0; the STATS tab rendered football's ATTACK/DEFENCE/KEEPING/DISCIPLINE groups and a page of NOT PUBLISHED, since the two sports share no stat keys, so `cricketStatGroups` is now picked from the package's own stat-dictionary categories; and the leaders' subtitle read UNKNOWN because ESPN's cricket position taxonomy has no 'Batter' and returns the literal string for 23 of the 81 ranked players. Also corrected a misuse of the season-archive flag: `archiveUnavailable` replaces the whole table with a 'use mobile' empty state, which is not what 'this sport has no archive' means. | `data/ipl-match-player-field-inventory.md`, `systems/predictions.md` | `tool/generate_cricket_league_stats.dart` (new), `cricket_league_stats_package_service.dart` (new), `league_stats_cubit.dart`, `team_stat_board.dart`, `league_detail_screen.dart`; `test/cricket_league_stats_package_test.dart` (new, 9 tests: alias resolution including that football must NOT resolve to the cricket package, the ten-team table ranked with points never rising as rank worsens, every board named/ranked/resolved, economy ranking ascending while every other board descends, rate boards stating their minimum, real leaders pinned (Sooryavanshi 776, Rabada 29), team stats with definitions, and the champions topping the table); existing `league_stats_package_service_test.dart` unchanged and green, proving football's path is untouched; clean `flutter analyze` on lib/, tool/ and test/; verified in the release web build under Playwright — TABLE, LEADERS and STATS all screenshotted and read. Full suite: 16 failures against a baseline of 18 earlier the same day; the one entry not in that baseline is another session's uncommitted `start-2nd-half` timeline filter, which takes the football EVENTS count from 23 to 22, and was left alone. |
| 2026-09-07 | Cyber UI standard box silhouette | Design-guideline update | PLANNED | Set the reference geometry for standard boxes, panels, and cards to opposing top-left + bottom-right cuts, with the other two corners square. Documented the existing `CyberClipper`/`CyberPanel` two-bottom-corner geometry as a legacy implementation mismatch and kept specialized control silhouettes exempt until a separate runtime migration. | `design/cyber-ui-design-system.md` | `.agents/skills/theme/SKILL.md`, `.agents/skills/cyber-ui/SKILL.md`; both skills passed `quick_validate.py`; wording audit across both skills and the canonical design page |
| 2026-09-07 | StatOz complete screen and state catalog | Product and QA documentation artifact | BUILT / PROTOTYPE DATA | Added a deterministic 393×852 production-widget capture harness, 45 PNG baseline scenarios covering every sport lane and all 18 game entries, JSON/CSV manifests, a source-file coverage audit, QA findings, and a searchable labeled PDF. Planned/deprecated and timing-dependent states are explicitly indexed rather than visually fabricated; Tennis Rally's observed loading frame is retained as QA-001. | `design/screen-state-catalog.md`, `README.md` | `tool/statoz_catalog_app.dart`, `tool/capture_statoz_catalog.ps1`, `tool/build_statoz_screen_catalog.py`, `test/screenshot_catalog/statoz_screenshot_catalog_test.dart`, `lib/app.dart`, `lib/utils/sound_effects.dart`; targeted Flutter analyze, release web build, PNG integrity/dimension audit, PDF render inspection and text extraction |
| 2026-09-07 | Match-page team name alignment | Visual hierarchy | BUILT | Stacked each team name centred under its own crest in the persistent match summary header, replacing the crest-then-name-beside-it row. Each side is now one identity block, the score is centred between them on the crest line, cricket keeps its innings score under the name in the same stack, and the status line, split team-colour rail, and corner brackets are unchanged. | `systems/predictions.md` | `match_summary_header.dart`, `match_summary_header_test.dart` (added a layout regression test asserting the name renders below the crest and shares its centre at 360 px width with a two-line club name); targeted Flutter analyze clean; 4/4 header tests pass; verified in a release web build under Playwright across finished, live, upcoming, and cricket fixtures. One pre-existing failure in `match_circle_widget_integration_test.dart` reproduces identically without this change (audioplayers plugin in a shared run). |
| 2026-09-07 | Football MATCH INTEL venue/attendance rows | Visual hierarchy | BUILT | Split the side-by-side VENUE + ATTENDANCE cells into two full-width stacked rows, so the venue string no longer competes with a fixed 96 px attendance column, and removed the `<WINNER> SECURED THE RESULT // <score>` footer line, which restated what the persistent match header already shows. League, season, panel accent, and every other OVERVIEW section are unchanged. | `systems/predictions.md` | `football_match_stats_view.dart`, `football_match_stats_view_test.dart` (added a layout test asserting ATTENDANCE renders below VENUE at the same left edge and that no SECURED THE RESULT text remains); targeted Flutter analyze clean; new test green; verified in a release web build under Playwright against the bundled Fulham-Chelsea package. The file's pre-existing `023 EVENTS` failure (another session's uncommitted start-2nd-half timeline filter) reproduces identically with this change stashed. |
| 2026-09-07 | Match header edge alignment and name truncation | Visual hierarchy | BUILT | Aligned each team block to its own outer edge of the match summary header — home left, away right, both 16px from the screen edge — by moving the frame inset from 20px outer plus 8px inner to a single 16px `_edgeInset` and giving `_TeamIdentity` an `alignEnd` cross-axis alignment. Team names now render on one line with ellipsis truncation instead of wrapping to two, so the crest-to-name stack keeps a constant height across fixtures. The cricket innings score under the name may still wrap, since truncating it would hide the result. Status line, score placement, split team-colour rail, corner brackets, and the separate Grand Prix header are unchanged. | `systems/predictions.md` | `match_summary_header.dart`, `match_summary_header_test.dart` (layout test rewritten to assert home crest left edge and away crest right edge both land 16px from the screen edge at 360px width, name left/right edges match their crest, and the name widget is `maxLines: 1` with `TextOverflow.ellipsis` on a single text line); targeted Flutter analyze clean; 4/4 header tests pass; verified in a release web build under Playwright across the IPL cricket fixture with a long club name plus finished, live, and upcoming football fixtures. |
| 2026-09-07 | Cricket match header innings split | Visual hierarchy | BUILT | Cricket's match summary header no longer prints the whole raw score string under the club name, where a chase line such as `161/5 (18/20 ov, target 156)` wrapped to two ragged lines. A new `_splitCricketScore` helper splits the string at its first parenthesis: the runs/wickets figure renders beside the crest on the crest line in tabular Orbitron, mirrored so the crest keeps the outer edge on the away side, and the qualifier renders as a muted uppercase label under the club name — the same prominent-score plus muted-detail split the SCORECARD innings header already uses. Scores with no parenthesis show runs only; fixtures with no score show crest and name only. The central `vs` mark, football/basketball/tennis centre scores, and the Grand Prix header are unchanged. | `systems/predictions.md` | `match_summary_header.dart`, `match_summary_header_test.dart` (cricket test rewritten to assert the combined raw string is gone, that runs and qualifier render as separate texts, that runs sit outside the crest on the crest line, that the qualifier sits below the name sharing its edge, and that the away block mirrors); targeted Flutter analyze clean; 4/4 header tests pass; verified in a release web build under Playwright across a finished chase, a finished first innings, an upcoming score-less cricket fixture, and two football fixtures. |
| 2026-09-08 | Football STATS: EVENTS tab retired into an OVERVIEW MATCH TIMELINE | Feature build and visual hierarchy | BUILT | Removed the football EVENTS tab (STATS is now OVERVIEW / MOMENTUM / LINEUPS / COMMENTARY) and rebuilt the event log as a **MATCH TIMELINE** section in OVERVIEW, taking the slot the GOAL IMPACT scorer list held. The scorer list is deleted rather than moved: every goal in the timeline already carries its running scoreline and assist, so `_GoalImpactPanel` and `_ScorerRow` were pure duplication of the same five moments. MOMENTUM's separate GOAL IMPACT MARKERS panel is untouched. The layout is the substance of the change. The old log was one left-aligned column whose only team signal was a 3px rail, so each row had to be re-parsed for its side. The timeline is now **two-sided around a centre spine**: minutes ride a continuous hairline down the middle, home events push left and away events push right, the glyph always hugs the spine with the copy reading outward, and a `FUL // MIN // CHE` legend states the split once. Type is carried by shape and semantic colour rather than the team accent: a booking is a drawn tilted card instead of a borrowed `Icons.style` glyph, and a substitution is the two names it actually is — on in `Cyber.lime`, off in `Cyber.danger`. Per THE GLOW RULE only the goal minute plate glows, which is defensible because a goal is the one event class that moved the score; goal plates also carry the new scoreline and the assist. Period markers cross the full width with their running score. Gratification: every row's ESPN prose is folded away until tapped, when it expands (`AnimatedSize` + selection haptic + `PressableScale`) pinned to that team's side behind an accent edge — reading a match becomes a sequence of small reveals rather than a wall of text, and 22 events stay legible on a 390px screen. Reused `ChamferedActionSurface` + `HudChamferClipper` for both the minute plate and the glyph so the diagonal cut motif holds, and `PressableScale` for the press punch; no new shared widget was needed. | `systems/predictions.md` (new 'Football MATCH TIMELINE (OVERVIEW)' section, tab list corrected, old EVENTS log-row paragraph replaced) | `football_match_stats_view.dart` (`_MatchTimelinePanel`, `_TimelineSides`, `_TimelineRow`, `_MinuteSpine`, `_EventBody`, `_EventGlyph`, `_EventReport`, updated `_PeriodMarker`; `_EventsSection`, `_EventLogRow`, `_GoalImpactPanel`, `_ScorerRow` deleted); `test/football_match_stats_view_test.dart` (8 tests, was 6: the five-view test became a four-view test and now asserts the timeline in OVERVIEW plus that both sides straddle the same centre line via `getRect` against the `MIN` label; the empty-feed test scrolls to `EVENT LOG PENDING` in OVERVIEW instead of selecting a tab; two new tests cover tap-to-expand-and-collapse of a goal's report and a substitution's lime-in/danger-out colouring); clean `flutter analyze` on the view and the test; verified in the release web build under Playwright via a temp `lib/_verify_football_timeline.dart` entrypoint mounted at `home:` (since deleted) — screenshotted the heading and legend, the full spine from KICKOFF to END REGULAR TIME, and both an away-side and a home-side report expanded. |
| 2026-09-08 | Penalty Shootout kick scene depth | Visual hierarchy | BUILT | The penalty taker was drawn on top of the goal. `_GoalGeom` put the goal line at `h * 0.80` and the penalty spot at `h * 0.93`, leaving only 28.6px between them at the 220px arena height, while the kicker rig draws ~114 units upward from its anchor — so at `mouthH * 0.52` the taker's head landed 27px **above** the goal line, overlapping the net and colliding with the LEFT/CENTER/RIGHT plates. Rebalanced the scene for depth at the user's chosen numbers (gap x3.2, arena 278px): crossbar `0.11`, goal line `0.616`, spot `0.945`, so the goal-line-to-spot gap goes 28.6px -> 91.5px while `mouthH` (140.6 vs 140.8) and the keeper's `mouthH * 0.78` height are effectively unchanged — the mouth did not shrink, the foreground grew. Posts, `targetY`, `zoneX`, the reticles and the three tap thirds keep their existing relationship to the mouth. The taker's scale is now **decoupled from the goal mouth**: a new `_GoalGeom.kickerH` (`sceneH * 0.33`) sizes him against the scene instead, since he is the nearer figure and should not shrink when the mouth does — 91.7px on the choose arena (was 73.2) and 101.5px on the result scene (was 78.8), which puts his head crown ~22px **below** the goal line in both. The two magic heights became `_kArenaHeight` (278) and `_kResultSceneHeight` (290, keeping the result scene's existing +12px headroom for the celebrate/dejected poses); both scenes live in a scrolling `MatchPhaseScaffold` list, so growing them lengthens the scroll rather than overflowing. A new local `_paintApproach` keeps the wider gap reading as distance rather than dead space: penalty-area sides splaying outward toward the camera, a squashed D above the spot, and turf contact marks under both figures — flat low-alpha `Cyber.cyan`, no glow per THE GLOW RULE, so the scene's only live glow stays on the selected reticle and the verdict stamp. First pass drew the contact marks as `Cyber.bg` shadows, which are invisible on a near-black ground; screenshots caught it and they became faint cyan scuffs in the pitch-line language. Also deleted the unused `GoalMouthFrame.scene` factory, which hardcoded the old 0.16/0.80 fractions and claimed in its doc comment to be "the shootout scene's proportions" — only `.diagram` is referenced (by the football shot map), so it was dead code that would have become a lie. | `games/penalty-shootout.md` (new scene-composition paragraph) | `lib/screens/shootout/widgets/penalty_goal_frame.dart`, `lib/widgets/cyber/goal_mouth.dart`; clean `flutter analyze` on both; `test/shootout_phase_test.dart` 9/9 green unchanged, including `'defence controls fit a narrow screen with large text'` (960x1920 @ textScale 1.4), which is the overflow guard for the 58px taller arena; verified in a release web build under Playwright via a temp `lib/_verify_penalty_arena.dart` entrypoint mounted at `home:` (since deleted) that renders the attack arena, the defence arena and the GOAL result cinematic in one page — screenshotted unselected, CENTER-selected (long shot-preview arc intact, no crossbar clipping) and the goal cinematic; taker clear of the mouth in all three. |
| 2026-09-08 | ESPN-powered EPL, LaLiga and IPL team hubs | Feature build and navigation | BUILT / SNAPSHOT + LIVE ESPN DATA | Made TABLE and STATS club rows open a season-scoped MATCHES / PREDICTIONS / PLAYERS hub. Generated packages now carry full football rosters, 380 fixtures per football league, all 202 IPL season participants and 74 IPL fixtures; native football routes merge fresher ESPN schedule/roster responses without blanking packaged data. Season fixtures enter the existing quiz lifecycle, football players reuse the season dossier, and IPL players open a season aggregate dossier. | `systems/predictions.md`, `README.md`, `design/screen-state-catalog.md`, `../data/league-stats-field-inventory.md`, `../data/ipl-match-player-field-inventory.md` | `team_hub_test.dart` package/merge/navigation/dossier coverage; targeted Flutter analyze; ESPN generator completeness guards and successful EPL/LaLiga/IPL regeneration. |
| 2026-09-08 | YOUR CLUB pin header trimmed | Visual hierarchy | BUILT | The match feed's followed-club pin header read `★ YOUR CLUB · CHELSEA` on the left and `FULL TIME` on the right, both of which restated what the fixture card directly beneath already shows — the card writes the club's own name in its identity colour and carries its own status tag. The header is now `★ YOUR CLUB` alone, and `_ClubPinReadout` returns nothing for a finished match. The readout's other states are unchanged and still earn their place, because they say something the card's tag does not: the pulsing LIVE NOW dot, KICKING OFF, and the sub-24h kickoff countdown (whose per-second ticker still only runs inside the last day). | `systems/predictions.md` (followed-club marking paragraph) | `lib/screens/predictions/prediction_home_screen.dart` (`_YourClubPinState.build`, `_ClubPinReadout.build`); clean `flutter analyze` on the file. |
| 2026-09-08 | F1 race weekend data: ESPN inventory + Italian GP package | Data extraction and documentation | BUILT (data only, no UI consumer) | Asked what F1 data ESPN provides per race and for a JSON file structuring the Italian GP. Probed the live API and found F1 has no `summary` feed at all (`.../racing/f1/summary?event=` 404s, unlike football and cricket), so a weekend is a reference graph walked over ~260 requests; also found `site.api.espn.com` returns 403 to an offline extractor for every sport, so the generator is built on `sports.core.api` plus `site.web.api` standings while the runtime service keeps using `site.api`. Added `tool/generate_f1_race.dart` (reuses `tool/espn_feed_client.dart`; `--race`/`--event`/`--season`/`--out`/`--check`, sorted keys so re-runs are byte-identical) and generated `assets/data/f1-italian-gp.json` (65.4 KB) for the 2026 Pirelli Italian GP: circuit with six SVG track maps, five sessions x 22-car classifications, 26 drivers (four reserves ran FP1 only), 11 constructors, and both championship tables with a per-race points grid over 25 rounds. Encoded ten upstream traps rather than passing them through — `pole` is a grid slot not a pole flag, qualifying `0.000` means eliminated, `totalTime` exists only for lead-lap finishers, `fastestLap` only for the one outright setter, and `avgSpeed`/`victoryMargin`/`poleSpeed`/`poleTime` are always `.000`. No screen consumes the package yet; motorsport fixtures still render from the live scoreboard path. | `systems/predictions.md` (new "F1 race weekend package" section), `../data/f1-race-field-inventory.md` (new) | `dart run tool/generate_f1_race.dart --race italian` then `--check` clean (proves deterministic extraction); clean `flutter analyze tool/generate_f1_race.dart`; asset spot-checked against the live API — race P1 driver 5829 grid 19 winner totalTime 1:51:15.281 fastestLap 1:23.504, P22 driver 5498 Retired on lap 1 with no totalTime, qualifying P11-P22 carry no Q3 key, circuit 53 laps / 11 turns / lap record 1:20.901, standings ITA 25 (driver) and 43 (constructor); JSON parses with all 110 classification driverIds resolving in `drivers`. |
| 2026-09-08 | Predictions / Football MATCH TIMELINE | Visual hierarchy | BUILT | Removed the `FUL // MIN // CHE` side legend and `TAP A MOMENT FOR THE FULL REPORT` caption from the Overview timeline. The centred minute spine and two-sided event layout remain, so the timeline starts directly below its section header. | `systems/predictions.md` | `football_match_stats_view.dart`, `football_match_stats_view_test.dart`; targeted Flutter analyze and widget test |
| 2026-09-08 | Profile / Talk to StatOz | Support and community surface | BUILT | Retired the Feedback & Enhancements form channel. Talk to StatOz now retains Bug, Feature Request, Score / Data Mismatch, and Shoutout, with renumbered compose telemetry, and adds a quiet Follow Us On panel for Instagram, Reddit, and YouTube. | `systems/tutorials-how-to-play-and-support.md` | `talk_to_statoz_screen.dart`, `talk_to_statoz_screen_test.dart`; targeted Flutter analyze and widget test |
| 2026-09-08 | F1 Italian GP STATS tab: charted race telemetry | Feature build and data visualisation | BUILT | Asked to build F1 stats for the Italian GP from the bundled package, with a leader-per-lap graph. **The lap chart could not be built and was not faked:** probing every candidate endpoint showed ESPN publishes no lap-by-lap data for F1 — `/laps`, `/details`, `/linescores`, `/splits` all 404, `plays`/`odds`/`officials` return `count: 0`, `probabilities`/`predictor`/`powerindex` 400 — and `lapsLead` is broken, totalling 4 across a 53-lap race. The agreed substitute is a **weekend position track** (FP1→FP2→FP3→QUAL→GRID→FIN, plotted inverted so P1 sits on top), whose crossing lines tell the same story from sessions ESPN actually recorded; Antonelli's 5→3→4→7→19→1 arc is the focal reveal. Motorsport was the last sport still on the legacy scoreboard branch, so it now gets `MotorsportMatchStatsView` mirroring the other three sport views (CyberFilterChips sub-tabs RACE / WEEKEND / QUALIFYING). No `MatchPulseHeader` — a Grand Prix is not a 1v1 — the hero is the circuit with ESPN's Monza track map, lap record and winner plate. Also ships gap-to-leader, pace evolution, qualifying Q1→Q2→Q3 elimination, and grid-vs-finish as a ranked signed **bar board** rather than a line, since drivers are separate categories. Two honesty fixes found during visual verification: the gap chart now plots lead-lap runners only (converting a lapped car's `behindLaps` to seconds drew a fictional 1208-second axis), and team-mates are drawn in lightened livery shades so a shared team colour doesn't read as one line doubling back. Races with no bundled package fall back to the legacy session/standings panels, which moved out of `match_detail_screen.dart` into the new file (~330 lines). | `systems/predictions.md` (new "The motorsport STATS tab" subsection; corrected the earlier "no screen reads it yet" statement), `../data/f1-race-field-inventory.md` (new "There is no lap-by-lap data" section with the full probe table) | New `lib/models/f1_race_package.dart`, `lib/services/f1_race_package_service.dart`, `lib/screens/predictions/widgets/motorsport_match_stats_view.dart`; additive `yAxisFormatter` on `lib/widgets/cyber/cyber_chart.dart` (defaults unchanged) so position and lap-time axes read `P19` / `1:22.6`. New `test/f1_race_package_service_test.dart` (10 tests against the real bundled asset, incl. a guard pinning `lapsLead` at 4 of 53 so the no-lap-chart constraint stays discoverable) and `test/motorsport_match_stats_view_test.dart` (6 tests: three sub-tabs, MatchPulseHeader absent, reserves excluded from the track, package-less fallback). Clean `flutter analyze` on all changed files; `test/cyber_chart_test.dart` + `match_detail_screen_test.dart` + the three sport stats-view suites green. Confirmed visually per CLAUDE.md via a temporary `lib/_verify_f1_stats.dart` entrypoint in a release web build under Playwright (since deleted), screenshotting all three tabs — which is how the 1208s axis and the team-mate colour collision were caught. Also fixed `test/favorite_club_pin_test.dart`, which the previous session's YOUR CLUB header trim had broken without being re-run. |
| 2026-09-08 | Motorsport moved ahead of Tennis in sport tab order | Navigation | BUILT | Reordered the sport strips so Motorsport sits before Tennis everywhere both appear: the canonical `sportTabOrder` (which drives the All Sports router, MATCH/GAMES strip labels/icons/colours, deck locker, match history and picks history), plus the three screens that keep their own list because they show different subsets — collection, leaderboard and shop. Order is now Football, Cricket, Basketball, Motorsport, Tennis. Safe to reorder because the active sport is persisted by enum NAME (`sportFromStorage`), never by tab index, and `hubIndexForSport`/`sportForHubIndex` derive from the list at runtime. **Known gap left as-is:** the compact MATCH/GAMES strip (`sport_underline_tabs.dart`) filters Motorsport out entirely (`_visibleSports`), showing four sports plus an ALL SPORTS overflow, so there was no ordering to change there — surfacing Motorsport on that strip would be a density change, not a reorder, and was flagged to the user rather than assumed. | `systems/predictions.md` (new canonical-order paragraph incl. the MATCH/GAMES exception), `systems/collections-decks-and-packs.md` (deck locker strip order) | `lib/config/sport_modules.dart`, `lib/screens/deck/all_cards_screen.dart`, `lib/screens/leaderboard/leaderboard_screen.dart`, `lib/screens/shop/shop_screen.dart`, `lib/screens/predictions/all_sports_screen.dart` (keyed map literal reordered for readability only). `test/sport_modules_test.dart` updated and extended to pin `sportTabOrder` itself, not just the colour sequence. Clean `flutter analyze` on all six files; `sport_modules_test` and `favorite_club_pin_test` green. Confirmed in a release web build under Playwright: SELECT SPORT lists Motorsport above Tennis, and the Shop and Leaderboard strips show the red helmet before the green racket. |
| 2026-09-09 | Italian GP made reachable; Motorsport restored to the MATCH strip | Navigation and data wiring | BUILT | Reported as "unable to access italian GP, and the changes in F1 and tab reordering changes in match tab". Two distinct causes, both confirmed by driving the running app. (1) The bundled Italian GP had **no fixture**: seeded motorsport stopped at the July GPs (British/Belgian/Hungarian) and the race could only arrive through the live ESPN scoreboard, which a web build cannot call (CORS) — so the motorsport feed opened on JUL 27-AUG 2 and the entire F1 STATS build was unreachable. (2) The compact MATCH/GAMES strip filtered Motorsport out entirely (`_visibleSports`), so it showed four sports plus an ALL SPORTS overflow and the previous day's tab reorder could not appear there. Fixes: added `F1RacePackageService.bundledFixtures()`/`fixtureFor()` mapping the package to a `SportMatch` (ESPN event id `600057442` so the STATS tab resolves the package off it, session lines rebuilt in ESPN's `"1. Driver · Constructor (time)"` format for the grid parser, home/away shaped like `_parseMotorsportEventToMatch` so a live response de-duplicates, retired cars reporting status instead of an empty bracket); wired it into `PredictionRepository.fixtures()`/`fixtureById()` on its **real** dates rather than `_keepBundledFixtureOnToday` (a Grand Prix is a dated event and the week picker already opens on the closest race week); added `_italianGpQuizzes` settled from the asset (Antonelli won, Gasly pole, Antonelli fastest lap) rather than reusing the generic `_f1Quizzes`, whose bonus options do not contain the real fastest-lap setter; and removed the Motorsport exclusion from the sport strip. | `systems/predictions.md` (F1 package section now covers fixture seeding; corrected the previous row's claim that the MATCH/GAMES strip deliberately omits Motorsport) | `lib/services/f1_race_package_service.dart`, `lib/services/prediction_repository.dart`, `lib/widgets/cyber/sport_underline_tabs.dart`. New `test/f1_race_package_service_test.dart` case pinning the derived fixture (id, real dates, five session names, `1. Pierre Gasly · Alpine (1:21.786)`, a Retired trailer, 23 standings rows) — 11/11 green; `motorsport_match_stats_view_test`, `sport_modules_test`, `favorite_club_pin_test`, `match_detail_screen_test` all green. Clean `flutter analyze` on the three changed files. Verified in a release web build under Playwright end to end: MATCH strip shows the red helmet before the green racket, MOTORSPORT opens on LAST WEEK / 4 SEP with "Pirelli Italian GP: Kimi Antonelli wins", both quizzes read FINAL RESULTS, and STATS renders the circuit hero, Monza track map and gap chart. `trending_hub_test` still fails identically to its pre-existing baseline (unrelated). |
| 2026-09-09 | MATCH sport strip curated to three sports | Navigation | BUILT | Asked to show only TRENDING, Football, Cricket, Basketball, the 3-dots overflow and search on the Matches sport strip, with Tennis moved into the 3 dots. The strip had briefly carried all five sports (added earlier the same day so Motorsport was not hidden), which crowded eight slots into the row. `_visibleSports` is now derived from a curated `_shortcutSports` set filtered through `sportTabOrder`, rather than "all sports" or "all but one": which sports earn a shortcut is a product call, and deriving it this way keeps the strip in the canonical order while stopping it silently growing when a sport is added to `sportTabOrder`. Motorsport and Tennis are both reached through ALL SPORTS, which continues to list every sport — the Italian GP remains reachable there, and the fixture-seeding fix from earlier today is untouched. | `systems/predictions.md` (replaced the strip paragraph, which had just been corrected in the opposite direction earlier today) | `lib/widgets/cyber/sport_underline_tabs.dart`. Clean `flutter analyze`; `sport_modules_test`, `favorite_club_pin_test`, `f1_race_package_service_test`, `motorsport_match_stats_view_test` and `match_detail_screen_test` all green. Verified in a release web build under Playwright: the strip renders exactly TRENDING / football / cricket / basketball / 3-dots / search, and the overflow lists Football, Cricket, Basketball, Motorsport, Tennis with Motorsport showing 4 fixtures in range. |
| 2026-09-09 | F1 feed-card readout: white title, P1-only result line | Visual polish | BUILT | Asked to keep the Grand Prix title ("Pirelli Italian Grand Prix") in white and trim the sub-heading to only "P1 : Kimi Antonelli". The title was dimming with the shared finished-match colour `_dimName` that team names use, which reads correctly for a side that lost but not for a race identity that doesn't have a losing side; it now always renders white and the now-unused `dim` parameter was removed from `_RaceIdentityRow`. The sub-heading was a full sentence built in `F1RacePackageService.fixtureFor()` ("Pirelli Italian GP: Kimi Antonelli wins"); reformatted to `'P1 : \$winnerName'`, and the seeded British GP fixture's resultLine ("British Grand Prix: Hamilton takes the chequered flag") was aligned to the same `P1 : <driver>` convention so every finished motorsport card on the same component reads consistently. | `systems/predictions.md` (F1 race weekend package section, new paragraph on the card readout convention) | `lib/screens/predictions/widgets/match_prediction_card.dart` (`_RaceIdentityRow`), `lib/services/f1_race_package_service.dart`, `lib/services/prediction_repository.dart` (seeded British GP). Clean `flutter analyze` on all three; `f1_race_package_service_test` (11/11, the existing `contains('Kimi Antonelli')` assertion holds under the new format), `motorsport_match_stats_view_test`, `match_detail_screen_test` and `favorite_club_pin_test` all green. Verified in a release web build under Playwright: the Italian GP card on the MOTORSPORT feed shows "Pirelli Italian Grand Prix" in solid white with "P1 : Kimi Antonelli" beneath it. |
